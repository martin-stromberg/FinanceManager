# Tasks: Einführung eines staging-Branches im CI-Prozess

| # | Bereich | Aufgabe | Status | Testnachweis |
|---|---------|---------|--------|--------------|
| 1 | Workflow-Konfiguration | `.github/workflows/test.yml` anpassen: `staging` zu `on.push.branches` hinzufügen | Erledigt | GitHub Actions läuft auf Push zu `staging` (Workflow ausgelöst via `on.push.branches: [master, staging]`) |
| 2 | Workflow-Konfiguration | `.github/workflows/test.yml` anpassen: `staging` zu `on.pull_request.branches` hinzufügen | Erledigt | GitHub Actions läuft auf PR gegen `staging` (Workflow ausgelöst via `on.pull_request.branches: [master, staging]`) |
| 3 | Workflow-Konfiguration | `.github/workflows/staging-to-master.yml` erstellen: Automatisierte PR-Erstellung von `staging` → `master` | Erledigt | `.github/workflows/staging-to-master.yml` vorhanden mit `workflow_run` Trigger; nutzt `gh pr create` mit Label `automated-promotion` und Draft-Status |
| 4 | Workflow-Konfiguration | Staging-to-Master-Workflow testen: Feature-Branch → PR gegen `staging` → automatische PR zu `master` verifizieren | Offen | Manuelle Verifikation erforderlich; Workflow ist implementiert, bedarf aber praktischer End-to-End-Validierung |
| 5 | Quality-Gates | Code-Coverage-Schwellwert 70% implementieren | Erledigt | `test.yml`: `--collect:"XPlat Code Coverage"` + ReportGenerator + „Enforce coverage threshold" Step mit 70%-Validierung |
| 6 | Quality-Gates | Dependabot-Integration aktivieren für NuGet, npm, GitHub Actions | Erledigt | `.github/dependabot.yml`: Alle drei Ecosystems konfiguriert, `target-branch: staging` gesetzt, wöchentliches Schedule |
| 7 | Branch-Schutz | GitHub Branch-Protection-Rules für `staging` konfigurieren (Status Checks erforderlich, Direct-Push blockieren) | Offen | Müssen manuell in GitHub UI oder via API konfiguriert werden (nicht im Code-Repository); Plan dokumentiert erforderliche Regeln |
| 8 | Branch-Schutz | GitHub Branch-Protection-Rules für `master` konfigurieren (Status Checks erforderlich, nur PRs erlaubt, optional: nur aus `staging`) | Offen | Müssen manuell in GitHub UI oder via API konfiguriert werden (nicht im Code-Repository); Plan dokumentiert erforderliche Regeln im Strict Mode |
| 9 | Dokumentation | README.md / CONTRIBUTING.md aktualisieren: Neuer Workflow-Beschreibung (PRs gegen `staging`, nicht `master`) | Erledigt | CONTRIBUTING.md Zeilen 103–108: „Branch-Workflow (staging / master)" Abschnitt; README.md Zeilen 185–258: „Deployment / CI/CD" aktualisiert |
| 10 | Dokumentation | Hotfix-Prozess dokumentieren (Hotfixes gehen über `staging`, nicht direkt zu `master`) | Erledigt | CONTRIBUTING.md Zeile 105: „Hotfixes gehen ebenfalls über `staging` (kein Direct-Push oder PR direkt gegen `master`)" |
| 11 | Dokumentation | Release-Prozess dokumentieren (Versionsbumps nur auf `master`, automatisierte PRs von `staging`) | Erledigt | CONTRIBUTING.md Zeile 107: „Versionsbumps (Semantic Release) erfolgen ausschließlich beim Merge zu `master`, nicht auf `staging`"; README.md dokumentiert Release-Pipeline |
| 12 | Team-Kommunikation | Team über neuen Workflow benachrichtigen (Slack, E-Mail oder Meeting) | Offen | Manuelle Aufgabe; Dokumentation ist vorbereitet (CONTRIBUTING.md, README.md) |
| 13 | Migration | Offene PRs gegen `master` überprüfen und zu `staging` umleiten oder schließen | Offen | Manuelle Aufgabe; Prüfung und Umleitung erforderlich |
| 14 | Verifikation | Lokal verifizieren: `staging`-Branch auschecken und verfolgbar machen (`git checkout --track origin/staging`) | Offen | Manuelle Aufgabe für Entwickler; Dokumentation in CONTRIBUTING.md vorhanden |
| 15 | Verifikation | Lokal verifizieren: Feature-Branch von `staging` erstellen und PR eröffnen; Test-Workflow sollte ausgelöst werden | Offen | Manuelle Verifikation; Workflow ist implementiert, bedarf praktischer Validierung |
| 16 | Verifikation | Lokal verifizieren: Nach Merge zu `staging` sollte automatisiertes PR zu `master` erstellt werden | Offen | Manuelle Verifikation; `staging-to-master.yml` ist implementiert, bedarf praktischer Validierung |
